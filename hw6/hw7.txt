К БД, которая была разработа на прошлой домашней работе, составить запросы на:
-выборку данных с JOIN, где должны использоваться LEFT, RIGHT, INNER, OUTER, CROSS JOINs;
-написать вложенные запросы: внутри SELECT и FROM, WHERE;
-написать запросы, которые используют группировку и сортировку записей.
-Написать запрос, используя группировку и вложенный запрос (можно воспользоваться оператором DISTINCT), который будет подсчитывать общее кол-во записей в группе.
-Написать вложенный запрос внутри FROM, в котором будет использоваться фильтр записей WHERE ..., например, отфильтрованные по статусу "active".